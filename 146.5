#include <iostream>
#include <string>
#include <fstream>
using namespace std;
int main()
{
int Z;
int i;
int count = 0;
string STR;
ifstream fileName("text.txt");
while (getline(fileName, STR))
{
        for (i = 1; i < STR.length(); i++)
        {
            if (STR[i] >= STR[i-1])
            {
                count++;
    s        }
        }
}
if (STR.length() == count+1) // т.к k меньше на 1
    cout << "po vozrastaniu";
else
    cout << "ne po vozrastaniu";
cout << STR;
return 0;
}
